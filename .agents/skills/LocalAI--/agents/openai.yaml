interface:
  display_name: "LocalAI"
  short_description: "Repo-specific patterns and workflows for LocalAI--"
  default_prompt: "Use the LocalAI-- repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true